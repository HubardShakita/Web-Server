<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
<body>
  <%- include('./partials/nav.ejs') %>
  <h1>Posts:</h1>
  <ul>
    <% if (posts.length) { %>
      <% posts.forEach(({ id, first_team, createdAt, text, second_team, match}) => { %>
        <li>
          <article>
            <h2>
              <a href="/posts/<%= id %>"><%= first_team %></a>
            </h2>
            <p><%= text%></p>
            <div class="info">
              <span><%= createdAt.toLocaleDateString() %></span>
              <span><%= second_team %></span>
            </div>
          </article>
        </li>
      <% }) %>
    <% } %>
  </ul>
</body>
</html>